services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: chicago_test_tui_desktop
      POSTGRES_USER: kickside
      POSTGRES_PASSWORD: kickside
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kickside -d chicago_test_tui_desktop"]
      interval: 2s
      timeout: 3s
      retries: 30
    tmpfs:
      - /var/lib/postgresql/data
