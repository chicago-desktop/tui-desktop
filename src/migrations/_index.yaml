version: "1.0"
namespace: chicago.tui_desktop.migrations

entries:
  # chicago.tui_desktop.migrations:01_create_windows
  - name: 01_create_windows
    kind: function.lua
    meta:
      type: migration
      comment: The table of windows built in the runtime; the loader brings them back from it after a restart.
      target_db: app:db
      timestamp: "2026-09-07T16:00:00Z"
    source: file://01_create_windows.lua
    imports:
      migration: wippy.migration:migration
    method: migrate

  # chicago.tui_desktop.migrations:02_window_group
  - name: 02_window_group
    kind: function.lua
    meta:
      type: migration
      comment: The menu folder of a workshop window — the menu_group column; empty means "not named".
      target_db: app:db
      timestamp: "2026-09-08T12:00:00Z"
    source: file://02_window_group.lua
    imports:
      migration: wippy.migration:migration
    method: migrate

  # chicago.tui_desktop.migrations:03_window_spec
  - name: 03_window_spec
    kind: function.lua
    meta:
      type: migration
      comment: The workshop window's description beyond the code — imports, icon, window type, pixel view; the spec column, JSON, '{}' means "nothing declared".
      target_db: app:db
      timestamp: "2026-09-09T09:00:00Z"
    source: file://03_window_spec.lua
    imports:
      migration: wippy.migration:migration
    method: migrate
