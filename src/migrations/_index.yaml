version: "1.0"
namespace: windows.tui_desktop.migrations

entries:
  # windows.tui_desktop.migrations:01_create_windows
  - name: 01_create_windows
    kind: function.lua
    meta:
      type: migration
      comment: Таблица окон, собранных в рантайме; из неё загрузчик поднимает их обратно после перезапуска.
      target_db: app:db
      timestamp: "2026-09-07T16:00:00Z"
    source: file://01_create_windows.lua
    imports:
      migration: wippy.migration:migration
    method: migrate

  # windows.tui_desktop.migrations:02_window_group
  - name: 02_window_group
    kind: function.lua
    meta:
      type: migration
      comment: Папка меню у окна из мастерской — колонка menu_group; пусто означает «не названа».
      target_db: app:db
      timestamp: "2026-09-08T12:00:00Z"
    source: file://02_window_group.lua
    imports:
      migration: wippy.migration:migration
    method: migrate

  # windows.tui_desktop.migrations:03_window_spec
  - name: 03_window_spec
    kind: function.lua
    meta:
      type: migration
      comment: Описание окна из мастерской сверх кода — импорты, значок, тип окна, пиксельный вид; колонка spec, JSON, '{}' означает «ничего не объявлено».
      target_db: app:db
      timestamp: "2026-09-09T09:00:00Z"
    source: file://03_window_spec.lua
    imports:
      migration: wippy.migration:migration
    method: migrate
